# Changelog

Format: `vX.X - one line - files touched`

- v0.1 - Camera, infinite grid, zoom/pan - `camera.js`, `grid.js`, `renderer.js`, `input.js`, `world.js`, `game.js`
- v0.2 - Tower placement (click to place, snapped to grid) - `tower.js`, `world.js`, `renderer.js`, `game.js`
- v0.3 - Enemy movement (waypoint-following, temporary hardcoded path + spawn timer) - `enemy.js` (new), `config.js`, `world.js`, `game.js`, `renderer.js`
- v0.4 - Combat (towers target/fire at enemies, damage, death events) - `combat.js` (new), `game.js`
- v0.5 - **Direction change: static-world multiplayer TD, not a path TD.** Removed `CONFIG.PATH`. Enemies now spawn at a random angle on a ring outside the world and walk straight at a central Base. New `base.js` (health/position) and `spawner.js` (replaces the planned `wave.js` — owns spawn timing, wave number, and weighted easy/medium/hard difficulty tiers). `combat.js` now also resolves base damage when an enemy reaches it. `renderer.js` draws the base instead of a path. - `base.js` (new), `spawner.js` (new), `enemy.js`, `world.js`, `combat.js`, `renderer.js`, `config.js`
- v0.6 - UI (score, wave/tier indicator, base health bar, FPS, win/lose banner) - `ui.js` (new), `index.html`, `css/style.css`, `game.js`
- v0.7 - **Phase 1 closeout + Phase 2a: Command Core MVP.** Fixed `world.score` (read by ui.js but never set/incremented — kills now add `enemy.maxHealth` to score). Added the interior base-building screen: 8x8 grid, 3 starter rooms (Reactor/AI Core/Storage) each with 3 upgrade tiers, toggled via `B` key. Rooms produce pooled `power`/`compute`/`storageCap` output (`commandCore.totals()`) — no direct combat effect yet, that's Phase 2b's job. World simulation (waves/combat) keeps running in the background while the Core screen is open. - `commandcore.js` (new), `room.js` (new), `config.js`, `game.js`, `input.js`, `renderer.js`, `ui.js`, `world.js`, `index.html`, `css/style.css`
- v0.7.1 - Moved the code-map/roadmap doc into the repo as `docs.html`, linked from the in-game HUD (`DOCS` button, top right). Added Phase 5 "Dev Tooling & Portfolio Polish" to the roadmap — commit-timeline/stats-chart About screen, self-syncing docs, test harness — filed as backlog, non-blocking. - `docs.html` (moved in), `index.html`, `css/style.css`
- v0.7.2 - Fix bug: `docs.html`'s tabs got stuck on Roadmap after the first visit — `renderRoadmap()` was wiping `.layout`'s entire innerHTML, permanently destroying the SVG/detail elements every other tab renders into. Swapped in a dedicated `#roadmap-slot` toggled by visibility instead of a destructive rebuild; also fixes phase-card detail clicks on the Roadmap tab, which had the same root cause. - `docs.html`
- v0.8 - **Phase 2b: Skeleton Economy.** Gold pool (`world.gold`, capped by `goldCap()`), kill rewards scaled by enemy tier, wave-clear bonuses, a tower build cost gate, and right-click sell/refund (60%). Command Core output is now a real modifier instead of a display-only number: Reactor(power) discounts tower cost, AI Core(compute) boosts gold rewards, Storage(storageCap) raises the gold cap — all read live via `commandCore.totals()`. `docs.html`'s Economy tab updated to mark the now-real nodes/edges. - `config.js`, `world.js`, `tower.js`, `spawner.js`, `input.js`, `game.js`, `ui.js`, `index.html`, `docs.html`
- v0.9 - **Phase 3: Full Core + Room Variety.** 5 new Command Core rooms (Lab, Factory, Hangar, Shield, Dock), each 3-tier like the originals. Lab produces `researchRate`, accruing into a spendable `CommandCore.research` stockpile (`commandCore.update(dt)`, mirrors how `world.gold` works). A 5-node tech tree (`CONFIG.TECH_TREE`) gates Factory → {Hangar, Shield} → Dock behind research spend, plus a parallel Module Slots node; Lab itself needs no node since it's the free root (no Lab built = no researchRate = tree never advances, gate enough on its own). Factory's `buildTimeReduction` shortens every future room's construction timer; rooms now take real time to finish and contribute nothing to `totals()` until built. Shield's `shieldPct` cuts base damage in `resolveBaseHits`; Hangar's `dronePower` is passive DPS applied every frame to whichever live enemy is closest to the base (no projectile/visual — abstract drones). Dock adds a manual gold→research trade button, ratio scaling with its tier. Module slots: tier-gated capacity (0/1/2), one universal +20%-output module per slot, gold cost scaling with tier & existing module count. Room building/upgrading/modding is now a real gold sink for **all** rooms (including the original 3 starters) — previously free. - `config.js`, `room.js`, `commandcore.js`, `world.js`, `combat.js`, `game.js`, `input.js`, `renderer.js`, `ui.js`, `index.html`, `css/style.css`, `docs.html`
- v0.9.1 - **Test harness (Phase 5 item, pulled forward).** Node is now installed on the dev machine, and Node 20 ships a built-in test runner — no npm/deps needed. 52 tests: `commandcore.test.mjs` (room placement/tiers/build timers, tech-tree gating incl. a cycle check, module slots), `economy.test.mjs` (tower + room/upgrade/module/dock-trade gold gating, Command Core → economy wiring), `combat.test.mjs` (tower targeting/cooldown, Projectile damage-on-arrival, base-hit `hasHitBase` guard, Shield/Hangar effects), `spawner.test.mjs` (wave state machine, difficulty-tier unlock gating). `balance.test.mjs` is the one this was really for: regression guards that catch the economy silently breaking as constants get tuned — `towerCost()`/Shield's damage reduction can't hit a 0 or ≥100% floor even at a maxed-out Core, purchase costs (`buildCost`/`upgradeCost`/`moduleCost`) must never get cheaper the more you buy, and the full tech tree must stay reachable in a bounded amount of Lab accrual time. Run via `node --test tests/` (`npm test` also works — added a minimal `package.json` for the script alias, but npm itself isn't on this machine, so `node --test` is the primary path). `docs.html`'s Phase 5 card marks the test-harness item done. - `tests/helpers.mjs` (new), `tests/commandcore.test.mjs` (new), `tests/economy.test.mjs` (new), `tests/combat.test.mjs` (new), `tests/spawner.test.mjs` (new), `tests/balance.test.mjs` (new), `package.json` (new), `docs.html`
- v1.0 - **Phase 4: Progression — Levels, Achievements & Prestige.** Persistent player profile (`localStorage`, survives run restarts), skeleton ported from a sister project's (RMUV) leveling/achievement/prestige system — stripped of everything that was really about RMUV's 40-game arcade catalog (avatars, cosmetics, genre shelves, cross-game score normalization); this game has one CP source, so it needed none of that. A single Cascade-Point spine (`profile.js`) fed by run events (kills, wave clears, towers placed, rooms built, tech unlocked, run win/loss) drives a gentle sqrt level curve. A data-driven achievement registry (`achievements.js`, 21 badges across common/special/epic/legendary) is tested via a fixpoint loop on every event so one unlock can cascade into another the same tick. Prestige resets the CP climb at an escalating level gate (10, 20, 30…) for a permanent +10%/prestige CP-gain bonus plus spendable skill points, sunk into a 4-node skill tree (Gold/Damage/Build/Fortification Mastery) that stacks with the existing Command Core bonuses. Runs can now actually be replayed: added a "Play Again" button (previously the win/lose banner was a dead end requiring a page reload) that rebuilds World/CommandCore while keeping the profile — this is also what prestige uses internally. New `P` key opens the profile view (level/XP, prestige, skill tree, achievement grid) alongside the existing `B` (Command Core) toggle. The engine files stay decoupled from the profile the same way `ui.js` already stays decoupled from gameplay mutation: `game.js` diff-watches a handful of monotonic counters (`world.kills`, `world.towersPlaced`, `spawner.wavesCleared`, `commandCore.rooms.length`, `commandCore.unlockedTech.size`) each frame and translates deltas into `profile.emit()` calls, rather than threading Profile through combat/spawner internals. 21 new tests (`profile.test.mjs`) cover the level curve, CP economy, achievement fixpoint unlocking, prestige reset/payout/gate escalation, skill cost curve, and the skill bonuses actually reaching World/combat formulas. - `profile.js` (new), `achievements.js` (new), `config.js`, `world.js`, `spawner.js`, `combat.js`, `game.js`, `ui.js`, `index.html`, `css/style.css`, `tests/profile.test.mjs` (new), `tests/helpers.mjs`, `docs.html`

- v1.1 - **Phase 5 (portfolio track).** Added `whathow.html`, a job-application-facing "what happens when..." doc: 13 real, shipped-only flows across 6 journeys, a Velocity section with real git-log-derived commit/LOC stats, and a Primitives section auditing the game's own design-system consistency — kept separate from `docs.html`'s internal roadmap/status tracker. The Primitives audit caught `.profile-panel` (added in Phase 4) duplicating the shared glass recipe line-for-line instead of joining the shared selector like every other surface — folded it in, deleted the duplicate block. - `whathow.html` (new), `css/style.css`
- v1.2 - **Phase 5 (portfolio track).** Merged `docs.html` (internal roadmap + code-map) and `whathow.html` (portfolio flows/Velocity/Primitives) into a single `whatever.html`, at the user's request, to stop maintaining two doc pages. Also re-themed: both prior doc pages had invented their own palettes (docs.html's own blue, whathow.html's pink/rounded-font) instead of matching the actual shipped game; `whatever.html` now uses the real primitives from `css/style.css` (bg `#09111B`, ink `#F2F8FF`, accent `#7ED9FF`, the exact glass recipe) that whathow's own Primitives section had already audited. Added a Roadmap section (ported from docs.html's phase cards, honest DONE/LATER/BACKLOG statuses kept, not hidden) between the hero and WhatHow; docs.html's Towers/Base/Enemies/Spawner/Economy node-graph tabs were dropped as redundant — cross-checked node-by-node, everything real in them was already superseded by whathow's existing flows, except Tower's "no health/can't be attacked" gap, which now gets a line in the `towerfires` flow and the Phase 4b roadmap card instead. Velocity's git-log snapshot refreshed for real (14 commits, ~51h across 4 days, category line counts recomputed via `wc -l`, fixing a small drift in the old numbers). `index.html`'s HUD `DOCS` button now points at `whatever.html`. - `whatever.html` (new), `docs.html` (removed), `whathow.html` (removed), `index.html`
- v1.3 - **Phase 4b: Economy Depth.** Combo streak (`world.comboStreak`/`comboTimer`, decays `COMBO_WINDOW` seconds after the last kill, stacks a gold-reward bonus folded into `rewardMultiplier()`), base passive income (gold/sec scaled by the persistent profile level, `world.updatePassiveIncome`), a Fast-Build rush (right-click a still-building Command Core room to pay gold and zero its timer, `world.rushBuildRoom` — reuses the build-timer state rooms already had, no new mechanism), a tower upgrade cost gate (towers now carry a 3-tier `CONFIG.TOWER_TIERS` damage/range/fire-rate curve, click a placed tower to upgrade it — same silent-no-op-if-you-can't-afford-it convention `upgradeRoom` already used), and a base repair button (`world.repairBase`, gold-gated, clamped to missing HP). The tower-upgrade UI reuses `index.html`'s `.tower-card` — a fully-styled aside that had sat `hidden` with hardcoded placeholder stats since the original HTML skeleton, never wired to any JS; it's now the live selected-tower inspector. Deliberately **cut from this pass**: the 6th item originally scoped for Phase 4b, tower health / tower-vs-tower or enemy-vs-tower damage — per the roadmap card's own caution ("new ground, not a lookup"), enemies still walk straight through towers to the base untouched; filed as its own unscoped backlog item instead of forced into this release. 20 new tests across `tests/economy.test.mjs` (combo decay/stacking, passive income accrual+cap, rush-build cost/gating, tower upgrade cost/gating, repair cost/clamp/gating) and `tests/balance.test.mjs` (tower upgrade cost monotonicity, rush cost always positive, combo bonus stays capped under an unbounded streak). - `config.js`, `tower.js`, `world.js`, `game.js`, `renderer.js`, `ui.js`, `index.html`, `css/style.css`, `tests/economy.test.mjs`, `tests/balance.test.mjs`, `whatever.html`
- v1.4 - **Phase 4c: Day-One Power & Metal.** Kills the "build a throwaway starter, then rebuild the real thing" onboarding trap: `CommandCore.placeStarterRoom`/`World.placeStarterScavenger` are new gate-bypassing methods, called only from `Game`'s constructor/`restart()`, that force-place a free, already-active Reactor and Scavenger Turret at world init. Deliberately kept OUT of `CommandCore`'s/`World`'s own constructors — those are also what ~30 existing unit tests call directly for a pristine fixture, very often building a fresh `'reactor'` at `(0,0)` as their go-to test room; baking the starter in there would have permanently broken `isBuilt('reactor')`/cell `(0,0)` for every one of them. New `metal` resource (second currency, `world.metal`/`metalCap()`/`addMetal()`) — **and combat Tower cost migrated onto it too**, not just the new Scavenger Turret: gold is now purely the Command-Core-room currency, metal funds everything on the exterior grid (`placeTower`/`sellTowerAt`/`upgradeTower` all swapped from gold to metal; Reactor's `CORE_POWER_COST_DISCOUNT_PER_POINT` now cheapens both). Scavenger Turret (`js/scavenger.js`, new) is a passive exterior placeable — no target/damage, 3-tier upgradeable like Tower, mines metal only through the new AI Cycle Budget scheduler. AI Core's whole identity reframed: the old flat `CORE_COMPUTE_REWARD_BONUS_PER_POINT` gold-reward bonus is gone, replaced by `cyclesPerMin` output driving `World.metalPerSecond()` — a continuous fair-share rate (not literal discrete queued cycles; mathematically the same long-run average, but deterministic and dt-driven like every other rate in the game) split evenly across active metal producers (Scavenger Turrets + the new Mine room). `BASE_CYCLES_PER_MIN` is an always-on bandwidth floor so metal accrues before any AI Core is built. Lab's `researchRate` deliberately stays OUTSIDE the scheduler (the roadmap text listed it as a competing producer, but retrofitting a tested Phase 3 mechanic for no real gain wasn't worth it — a call made with the user before implementation). Also replaced 5 fully-dead placeholder buttons in `index.html`'s field build-bar footer (Cannon/Sniper/Laser/Missile/Slow — leftover scaffolding, never wired to anything, no relation to the actual single-tower-type game) with 2 real, wired Tower/Scavenger slots. 26 new tests; the first draft of `balance.test.mjs`'s day-one-pacing test caught `BASE_CYCLES_PER_MIN: 1` + tier-1 `metalPerCycle: 1` needing 25 real minutes to afford a second Scavenger Turret — tuned to `3`/`3` (~167s) before landing. `CommandCore.totals()`'s `compute` key renamed `cyclesPerMin`; `ui.js`'s Core Panel "Reward Bonus" stat removed (no longer Core-driven) and replaced with "Metal/sec". - `config.js`, `commandcore.js`, `scavenger.js` (new), `world.js`, `game.js`, `renderer.js`, `ui.js`, `index.html`, `css/style.css`, `tests/helpers.mjs`, `tests/economy.test.mjs`, `tests/balance.test.mjs`, `whatever.html`
- v1.5 - **Bugfix + Market room (gold↔metal trading).** Fixed a real regression from v1.4: inserting `mine` into `CONFIG.ROOM_TYPES` (between `lab` and `factory`) shifted every room type after it by one position, but `game.js`'s number-key handler (`['1'..'8']` + `Object.keys(CONFIG.ROOM_TYPES)[Number(key)-1]`) and `index.html`'s `core-build-bar` labels never moved with it — key `5` silently built Mine while still labeled "Factory", keys `6`-`8` were each off-by-one, and **Dock was completely unreachable** (needed key `9`, outside the checked range). Caught while scoping this session's work, not from a bug report — `game.js`/`index.html` have zero unit-test coverage (DOM/canvas-dependent). Fixed with an explicit `keyOrder` array (`'1'`-`'9'`,`'0'`) instead of key-to-index arithmetic, `core-build-bar` relabeled to match `ROOM_TYPES`' actual order, and a new `balance.test.mjs` regression guard capping `ROOM_TYPES` at 10 entries (as many as the key scheme can address) so a future room addition can't repeat this silently. Then the actual feature: a free-root Market room (no tech gate, available from minute one like Reactor/Lab/Mine) with two manual trade directions, `world.tradeGoldForMetal()`/`tradeMetalForGold()`, mirroring Dock's existing gold→research trade pattern exactly (`MARKET_TRADE_GOLD_COST`/`MARKET_TRADE_METAL_COST`/`MARKET_TRADE_BASE_RATIO`, ratio improved by Market's tier). Scoped down on purpose from a bigger "market transactions" idea floated mid-session (buy/sell materials, research-linked bonuses, a module type per structure) — that stays an unbuilt idea in Next-up below, not a phase. `ui.js`'s trade-button wiring is fully generic-pattern reuse (constructor refs + `hidden`/`disabled` toggling), zero new CSS. 7 new tests, including one that would have caught the key-mapping bug had it existed a session earlier. - `js/game.js`, `js/config.js`, `js/commandcore.js`, `js/world.js`, `index.html`, `js/ui.js`, `tests/economy.test.mjs`, `tests/balance.test.mjs`
- v1.6 - **Phase 4d: Energy System.** Reactor's `power` stops being a one-time permanent discount on Tower/Scavenger cost (`CORE_POWER_COST_DISCOUNT_PER_POINT`, gone, same full-replacement treatment `CORE_COMPUTE_REWARD_BONUS_PER_POINT` got in Phase 4c) and becomes a live supply pool combat Towers draw from continuously — `World.powerSupply()`/`powerConsumption()`/`powerFactor()`, same fixed-supply/split-across-consumers shape as Phase 4c's AI Cycle Budget, but a pure derived ratio (no `update(dt)`/accrual needed, unlike metal). When total tower consumption (`CONFIG.TOWER_POWER_CONSUMPTION`, tier-scaled) exceeds Reactor's supply, `combat.js` stretches every tower's fire-rate cooldown by the shortfall ratio instead of the base absorbing full damage from an under-powered defense — floored at `BROWNOUT_MIN_FIRE_RATE_MULT` so towers slow down but never fully stop, same not-quite-zero philosophy as Shield/Fortification's damage-reduction caps. `towerCost()`/`scavengerCost()` are flat constants now — Reactor's value proposition shifted from "cheaper turrets" to "keeps your turrets firing at full rate." Scope calls made without re-litigating (same reasoning Phase 4c's Lab-exclusion used): only combat Towers consume power (Scavenger Turret is passive, never fires; Command Core rooms aren't consumers), a tower always draws its full consumption once placed regardless of whether it currently has a target (matches Phase 4c's "built = active, no has-work check"), and brownout only throttles fire rate — the roadmap's "reduced accuracy, disabled abilities" wording doesn't map to any mechanic that exists yet (no miss-chance, no active-ability system; that's Phase 6). New Core Panel "Power Draw" row and a HUD brownout badge (shown in field view whenever `powerFactor() < 1`). Two pre-existing tests broke and got fixed to match new reality, not hacked around: `combat.test.mjs`'s first firing test implicitly assumed full power with no Reactor built (now builds one, matching its actual intent); Phase 4c's "day-one guarantee" test asserted the starter Reactor discounted tower cost (no longer true — repointed at `powerSupply() > 0`). Per this session's explicit direction, these numbers (`TOWER_POWER_CONSUMPTION: [2, 3.5, 5]`, `BROWNOUT_MIN_FIRE_RATE_MULT: 0.25`) are a first working pass, not a tuned balance point. - `js/config.js`, `js/world.js`, `js/combat.js`, `js/ui.js`, `index.html`, `css/style.css`, `tests/economy.test.mjs`, `tests/combat.test.mjs`, `tests/balance.test.mjs`, `whatever.html`
- v1.7 - **Phase 5: git-hook-baked stats.json.** `stats.json`'s growth/category numbers stop being a hand-authored snapshot — `scripts/gen-stats.mjs` walks the full git log (`git ls-tree`/`git cat-file`, cached by blob SHA) and computes both from real history, then re-inlines the same object into `whatever.html`'s `STATS` block (between `STATS_GENERATED_START`/`END` markers). A pre-commit hook (`scripts/hooks/pre-commit`, tracked source — git doesn't version `.git/hooks` itself, installed via `npm run hooks:install`) runs it before every commit and stages both files, so Velocity can't go stale again. Covers all 20 commits at ship time instead of the old hand-curated 14. - `scripts/gen-stats.mjs` (new), `scripts/hooks/pre-commit` (new), `stats.json` (new), `whatever.html`, `package.json`
- v1.8 - **Phase 5: in-game About/Analytics panel.** New `O`-key/`ABOUT`-HUD-button view reading `stats.json` live (`js/stats.js` fetches + caches it once; `js/ui.js` renders it once on load, not per-frame, since the data is a static snapshot for the session) — generated date, total lines, a category breakdown, the last 12 commits. Category bars deliberately reuse the game's one real accent color (`#7ED9FF`) at four opacity steps instead of inventing new hues, to keep the Primitives section's "one accent color" claim true; that section's surface count/enumeration updated accordingly (six → seven shared-glass surfaces), and a new WhatHow flow (`statspipeline`, "Proving it" group) documents the commit → hook → `stats.json` → {doc page, in-game panel} pipeline end to end. Also filed a new unscoped roadmap card, Phase 5b (avatar-dropdown → Account/Settings/Inventory/Base modal, floated mid-session) — explicitly captured, not built. - `js/stats.js` (new), `js/ui.js`, `js/game.js`, `index.html`, `css/style.css`, `whatever.html`
- v1.9 - **Phase 5: Roadmap ↔ changelog drift check.** `whatever.html`'s ROADMAP array can't be mechanically regenerated from this changelog (this file is hand-written prose per version, not per-phase structured data — several versions map to one phase, e.g. v0.7/v0.7.1/v0.7.2 all being Phase 2a), so instead of overpromising a full auto-sync, `scripts/check-roadmap-sync.mjs` cross-checks "Phase N" mentions in both files and warns on drift either direction. Advisory only, wired into the same pre-commit hook as the stats generator — never fails a commit, just surfaces the mismatch. Caught a real one immediately: Phase 5b (added in v1.8, above) had no changelog mention until this entry. - `scripts/check-roadmap-sync.mjs` (new), `scripts/hooks/pre-commit`, `package.json`, `changelog.md`
- v2.0 - **Phase 5: /docs folder — closes out the phase.** `docs/architecture.md` (module map, the per-frame orchestration order, the three economy layers and how the AI Cycle Budget/power-brownout schedulers share one shape, persistence/gate-bypassing-starter patterns, testing philosophy including what has zero coverage and why, the doc-tooling pipeline, known non-goals) and `docs/CONTRIBUTING.md` (setup, commands, a pre-commit checklist, codebase conventions like "config.js is the only place tunables live" and "file a roadmap card instead of just discussing an idea"). All six Phase 5 roadmap items are now done; `whatever.html`'s Phase 5 card flips from `BACKLOG · PARALLEL` to `DONE` (Phase 5b, the player-menu-shell idea, stays its own separate unscoped card). Also corrected a stale number caught while updating the card: the test-harness item still said "76 tests across 7 files" from v0.9.1 — it's 114 across 6 test files now. - `docs/architecture.md` (new), `docs/CONTRIBUTING.md` (new), `whatever.html`
- v2.1 - **Phase 5b, scoped slice: avatar dropdown + real Settings tab.** A HUD-corner avatar button (top-right) opens a dropdown (Profile/About/Settings/Reset Progress) — purely additive, the existing B/P/O hotkeys still work untouched; the menu just calls the same view-toggle callbacks. New Settings `aside` (S hotkey or the menu): Appearance (light/dark theme toggle, `data-theme` attribute + localStorage, most of `style.css`'s chrome re-themed via CSS custom properties — semantic accents like tier colors and the HP-critical red stay fixed on purpose) and Save Data (storage key + live byte size, a raw-JSON viewer of the profile blob, Delete Save Data behind a shared confirm modal that calls `profile.hardReset()` then restarts the run). Did NOT attempt the tabbed-modal IA redesign or an Inventory tab — both still unscoped, see the updated Phase 5b roadmap card. - `js/profile.js` (`hardReset()`, `rawJSON()`), `js/ui.js`, `js/game.js`, `index.html`, `css/style.css`, `tests/profile.test.mjs`, `whatever.html`
- v2.2 - **`whatever.html` tab nav + roadmap accordion; 10 new WhatHow flows; fix stuck reset-confirm modal (issue #1).** Restructured the doc page from one continuous scroll into 5 hash-addressable tabs (Roadmap/WhatHow/Velocity/Primitives/Built), reusing the existing single-accent pill styling rather than a stock screenshot's literal multi-hue nav. Roadmap is now an accordion — DONE phases collapse by default, active/backlog ones stay expanded — and gained Phase 5c, an unscoped idea for in-game GitHub-issues bug reporting. Closed a real documentation-debt gap found by grepping `FLOWS` for shipped mechanics: 10 new flows written from actual source, covering module slots, Fast-Build rush, tower upgrades, base repair, the metal economy/AI Cycle Budget, Market trading, power/brownout, combo streak, passive income, and Phase 5b's avatar menu + Settings + save-data management. Also fixed 4 stale numbers (test count/files, Primitives surface count — 10, not "six/seven" — Velocity's commit/hour/test-ratio stats) and wired the commit count to compute from `STATS.growth.length` at render time instead of hand-typing it, after that exact hand-typed number drifted stale again within one commit. Root-caused and fixed issue #1: `.confirm-modal-overlay` was missing a `[hidden]{display:none}` override — same bug class that previously bit `.repair-btn` (v1.3) and `.tower-stat`/`#tower-combat-stats` — so every JS close path (Cancel/OK/backdrop click) correctly set `.hidden`, but CSS never honored it and the modal could never actually be dismissed. - `whatever.html`, `css/style.css`

## Next up

- Phase 5c (new, deliberately unscoped) - In-game bug reporting via GitHub Issues: floated 2026-07-19 right after issue #1 got filed the manual way. Real blocker before this can be scoped — a static client-side game has nowhere safe to hold a GitHub API token. Three options on record, none decided: a serverless proxy, GitHub's own pre-filled "new issue" URL scheme (no token, but leaves the game), or skipping GitHub entirely. See the `whatever.html` Roadmap card for full detail.
- Backlog (new, not scoped) - Market transactions, full vision (user's idea): v1.5 shipped the small first step — a Market room trading gold\<->metal. Still unbuilt and unshaped: research-linked market bonuses, and "a plethora of mods" per structure — today every room type shares one universal +20%-output module (Phase 3, deliberately kept simple), the user's vision wants structure-specific module variety instead. Natural next place to look for the module-variety half: `CONFIG.ROOM_TYPES[type].modules` becoming per-type instead of `CONFIG.MODULE_BONUS_PCT` being one global constant.
- Phase 5b (partial, v2.1) - Player menu shell: the avatar dropdown and a real Settings tab are done (see v2.1 above). Still unscoped: replacing the four hotkey-toggled panels with an actual tabbed modal, an Inventory tab (no item/equipment system exists at all yet), and merging the level badge into the avatar itself. Closer to a from-scratch IA redesign than an extension — same caution as before.
- Phase 6 (new) - Comms & Station Identity: from the user's lore docs — a Communications room unlocking 5 active player-triggered orbital abilities (EMP/Orbital Laser/Supply Drop/Drone Repair/Satellite Recall, the first mid-wave player input the game has), plus a station skin that visibly grows (Orbital Platform → ... → Dyson Node) off profile.js's existing prestige-level gate, no new state needed.
- Phase 7 (new, deliberately unscoped) - Satellite Identity: from the user's `satellites.md` — rename Tower → Satellite, split the single generic tower type into 9 classes with per-satellite XP/evolution and modules (Weapon/Power/Cooling/AI Chip/Targeting/Armor). Biggest lift and payoff of any open idea, filed without a designed shape on purpose — hard pre-condition is a real playtest pass on the existing single-satellite-type balance curve first (see the difficulty-curve note below, still blocking). Full reasoning: `dreams/2026-07-19-fiction-vs-implementation-gaps.md`.
- Backlog (new, deliberately unscoped) - Tower health / tower-vs-tower or enemy-vs-tower damage: cut from Phase 4b on purpose (see the v1.3 entry above). Tower still has no health field or `takeDamage()`, and enemies still walk straight through towers to the base untouched. Needs real design before it's buildable — does it replace the walk-to-base behavior, splash vs. single-target, can towers be destroyed — not just a lookup against existing patterns the way Phase 4b's other items were.
- Later - real difficulty curve tuning for `DIFFICULTY_TIERS` (needs actual playtesting, now also needs to account for the Phase 3 room-cost/build-timer economy, Phase 2b's gold economy, and Phase 4's skill-tree multipliers all stacking); multiple simultaneous bases/players for the multiplayer goal; revisit whether prestige should also reset `life` stats for an RMUV-style mastery-star re-earn (skipped this pass — see the v1.0 note below)

## Notes for next step

- **Phase 4 scope cut, on purpose:** RMUV's prestige also wipes per-game play stats so achievements re-earn across "lifetimes" (mastery stars, gold/silver/bronze per re-earn). That's real design value but real added complexity — a parallel life/permanent stat split. Skipped here: `profile.js`'s `life` stats and earned badges are permanent, prestige only resets `cp`. Revisit if prestiging more than 2-3 times ends up feeling like "nothing changed except a number."
- `profile.js`'s Node-safe storage fallback matters more than it looks: Node has no global `localStorage`, so `Profile` falls back to a per-instance in-memory store (NOT a shared module singleton — an earlier draft got this wrong and leaked state between test files) when `typeof localStorage === 'undefined'`. `World`/`freshGame()` in `tests/helpers.mjs` both default to a fresh `Profile` so every pre-existing test kept working unmodified.
- Couldn't screenshot-verify the profile panel/toast/restart-button UI in this environment — same sandboxed Chrome `ld.so` relocation crash noted since v0.8, unrelated to this change. Verified instead via the Node harness (`profile.js`/`achievements.js` have zero DOM dependencies) plus a manual cross-check that every `ui.js` `getElementById` call resolves against `index.html`.
- `spawner.js` picks difficulty tiers by weighted random from `CONFIG.DIFFICULTY_TIERS`, gated by `unlockWave`. This is deliberately simple — replace with real curve design once balance passes start.
- Base is currently single, at world center (0,0). Multiplayer/multi-base is out of scope until the single-base loop is fully working.
- Command Core still only allows one of each room type (`CommandCore.isBuilt`), now across 8 types instead of 3. Deliberately kept for Phase 3 rather than switching to build-many-of-each — tier upgrades + module slots already give each room type a progression curve, and one-of-each keeps the 8x8 grid from becoming a stacking optimization problem. Revisit if a future phase wants base-building depth beyond that.
- Couldn't screenshot-verify the new rooms/tech-tree/module UI in this environment — the sandboxed Chrome binary still crashes on launch (`ld.so` relocation failure), same as noted in v0.8, unrelated to this change. Verified instead with a Node harness (`js/config.js`, `room.js`, `commandcore.js`, `world.js`, `combat.js` have zero DOM dependencies, so they import and run directly under plain Node) that built rooms, ticked build timers and research accrual, unlocked tech nodes in dependency order, installed a module and checked its output bonus, and asserted Shield's damage reduction and Hangar's drone DPS against the actual numbers combat.js produced — plus a manual ID cross-check between `ui.js`'s `getElementById` calls and `index.html`. The DOM-dependent wiring (`ui.js` event listeners, `renderer.js` canvas layout, `game.js` input routing) was verified by code trace only, not a live click-through.
- Room build/upgrade/module costs and the build timer apply uniformly to all 8 room types, including the original Phase 2a starters (Reactor/AI Core/Storage), which were previously free and instant. This was a deliberate call to keep the Core's economy internally coherent now that it's a real gold sink — not called out as a balance concern since nothing has been playtested yet anyway (see the difficulty-curve note above).
- Shield and Hangar are combat-side modifiers (read via `world.commandCore.totals()` directly in `combat.js`), not routed through `world.js`'s gold-focused economy helpers — they don't touch gold, so they didn't need a `world.js` wrapper the way tower/room costs do.
- `docs.html`'s Towers/Base canvas flow diagrams (the node-graph tabs, not the Economy web) still don't show Shield's damage-reduction or Hangar's drone-DPS steps — only the Economy tab and Roadmap were updated. Low priority since those diagrams are about the tower/base combat path specifically, and Shield/Hangar hook in as flat modifiers rather than new steps in that flow, but worth a pass later if the diagrams start feeling stale.
- `World` now takes `commandCore` in its constructor (`new World(commandCore)`) so it can read `commandCore.totals()` for cost/reward/cap formulas — if `World` is ever instantiated elsewhere (tests, etc.), it needs a `CommandCore` instance passed in.
- Tower sell is right-click on a placed tower (60% refund of what was actually paid, tracked per-tower as `tower.cost`) — no confirmation dialog, matches the no-friction feel of placement.
- The test suite's own first draft had 2 bugs, both in test code, not game code: `balance.test.mjs`'s "max out a room" helper tried to build Shield/Factory/Hangar without unlocking their tech first (those room types return `null` from `placeRoom` when locked, same as the real game would refuse); fixed by unlocking the prereq chain before placing. `commandcore.test.mjs`'s cycle-detection check used a single global "seen" set across the whole tree walk, which flags any node reached twice as a "cycle" — but `dockAccess` legitimately reaches `factoryAccess` via two different branches (`hangarAccess` and `shieldAccess`), a valid diamond dependency, not a cycle. Fixed by tracking only the current DFS path per branch instead of a global visited set. Worth remembering if the tech tree grows and a real cycle check is needed elsewhere.
- Phase 4c's starter Reactor/Scavenger Turret are placed via explicit `placeStarterRoom`/`placeStarterScavenger` calls in `Game` only — never inside `CommandCore`'s/`World`'s own constructors, and `tests/helpers.mjs`'s `freshGame()` doesn't call them either. If a future phase adds another "free starter X" (a lore doc hints at more), keep following this pattern: constructors and `freshGame()` stay pristine, `Game.placeStarters()` is the one place real-game starter state gets injected. Tests that specifically want a starter call the gate-bypassing method themselves.
- Phase 4c deliberately left Lab's `researchRate` outside the AI Cycle Budget scheduler even though the roadmap text listed it as a competing producer ("whatever's built: turret, lab, mine") — a call made with the user before implementation, not a unilateral cut. Revisit only if a future pass wants research to compete with metal for AI attention; as shipped, Lab and the Cycle Budget are fully independent systems that happen to share the same room-tier/`researchRate`-vs-`metalPerCycle` shape.
- Combat Tower's cost moved from gold to metal this phase (not just the new Scavenger Turret) — a bigger, explicitly-chosen call over the lower-risk "leave Tower on gold, metal is Scavenger-only" alternative. Gold is now purely the Command-Core-room currency; metal funds the whole exterior grid. If this ever feels wrong in play (untested — see the difficulty-curve note above), the lower-risk alternative is still on record as the option not taken.
- Couldn't screenshot-verify the v2.1 avatar menu/settings panel/theme toggle — same sandboxed Chrome `ld.so` relocation crash noted since v0.8 (now also confirmed with no `npm`/Playwright/`chromium-cli` available in this environment either, so there was no automated-browser fallback this time). Verified instead by: `node --test tests/` (116/116 passing, incl. two new `hardReset()`/`rawJSON()` cases), `node --check` on every touched `.js` file, a Python pass confirming all 89 `index.html` element `id`s are unique, and a manual cross-check that every new `ui.js` `getElementById` call resolves against `index.html`. The DOM-dependent wiring itself (avatar-menu open/close, theme toggle, confirm-modal flow) was verified by code trace only, not a live click-through.
- `hardReset()` deletes save data by overwriting it with `blank()` and calling the existing `save()`, not by adding a `removeItem()` to the storage interface — the Node-fallback memory store only implements `getItem`/`setItem`, and a blank saved profile is functionally identical to no profile at all from the app's perspective.
- Theme only re-skins the shared chrome (page background, glass panels, primary/dimmed text, hairlines, the one recurring accent color) via CSS custom properties in `style.css` — semantic accents (combo gold, brownout/HP-critical red, achievement tier colors) stay fixed across both themes on purpose, same as most real apps leave status colors alone across light/dark rather than re-deriving them.
- The avatar dropdown's Profile/About/Settings items call the exact same `onToggleProfile`/`onToggleAbout`/`onToggleSettings` callbacks the B/P/O/S hotkeys already used — it's a second entry point onto the same `Game.view` state machine, not a new one, so nothing about existing keyboard-driven navigation changed.

